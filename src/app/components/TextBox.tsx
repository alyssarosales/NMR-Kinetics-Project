import {useState, useCallback } from 'react';

/* export default function Search() {
    function search(formData: string) {
        'use server';
        const query = formData.get('query');
        alert(`This worked. You typed ${query}`);
    }

    return (
        <form action={search}>
            <input name = 'query' />
            <button type = 'submit'>Search</button>
        </form>
    );
    } */
